# Wo Bot 架构图

> 最后更新：2026-07-19 | 对应版本：wo-bot-control v0.2.5

---

## 一、整体系统架构

```mermaid
graph TB
    subgraph 用户端["用户端 (客户端)"]
        Web["🖥️ wo-bot-web-debug<br/>Vue 3 + Vite<br/>浏览器控制台"]
        App["📱 wo-bot-app<br/>Flutter + Riverpod<br/>移动端 App (骨架阶段)"]
    end

    subgraph 云端服务["☁️ 云端服务"]
        Account["🔐 wo-bot-account<br/>NestJS + Logto<br/>帐号 & 设备绑定"]
        Signal["📡 wo-bot-signal<br/>WebSocket 信令中继<br/>（规划中）"]
        Market["🛒 wo-bot-market<br/>Python HTTP<br/>软件市场"]
        TURN["🔄 coturn TURN/STUN<br/>NAT 穿透中继"]
    end

    subgraph 机器人端["🤖 机器人端 (Jetson Orin)"]
        Control["⚙️ wo-bot-control<br/>Python asyncio<br/>主控制服务"]
        subgraph 硬件["🔌 硬件外设"]
            Camera["📷 摄像头<br/>USB + CSI"]
            Gimbal["🎯 云台<br/>二自由度舵机"]
            Motor["🛞 电机驱动<br/>麦轮/差速/阿克曼"]
            Mic["🎤 麦克风"]
            Speaker["🔊 扬声器"]
            Display["🖥️ 显示器"]
        end
    end

    Web <-->|"WebSocket :8765<br/>信令 + 命令"| Control
    Web <-->|"WebRTC P2P<br/>视频流 + DataChannel"| Control
    Web -.->|"mDNS 发现<br/>_wobot._tcp"| Control
    Web -.->|"HTTP :8000<br/>MJPEG/截图/状态"| Control

    Control -->|"设备注册 & 心跳"| Account
    Control -->|"软件包查询"| Market
    Control -.->|"跨网络信令 (规划)"| Signal

    Control --> Camera
    Control --> Gimbal
    Control --> Motor
    Control --> Mic
    Control --> Speaker
    Control --> Display

    App -.->|"mDNS / WebSocket"| Control
    App -.->|"OAuth2 登录"| Account
```

---

## 二、机器人端 (wo-bot-control) 模块架构

```mermaid
graph TB
    subgraph 入口["入口"]
        Main["WoBotControl<br/>main.py<br/>主控制器 & 生命周期"]
    end

    subgraph Core["Core 核心服务层"]
        WS["WebSocketServer<br/>websocket_server.py<br/>客户端连接入口 :8765"]
        RTC["WebRTCService<br/>webrtc_service.py<br/>视频推流 + DataChannel"]
        MsgHandler["MessageHandler<br/>message_handler.py<br/>消息路由中枢"]
        MDNS["MDNSService<br/>mdns_service.py<br/>mDNS 局域网发现"]
        HTTP["HttpAPIServer<br/>http_api.py<br/>HTTP API :8000"]
        SvcMgr["ServiceManager<br/>service_manager.py<br/>子进程守护管理"]
        BindMgr["BindingManager<br/>binding_manager.py<br/>HMAC 绑定认证"]
        AcctClient["AccountClient<br/>account_client.py<br/>帐号服务器对接"]
        PeriDetect["PeripheralDetector<br/>peripheral_detector.py<br/>外设可用性检测"]
    end

    subgraph Modules["Modules 功能模块层"]
        Motion["Motion 运动控制<br/>motion/motion_controller.py<br/>麦轮/差速/阿克曼"]
        Camera["Vision 视觉<br/>vision/camera.py<br/>OpenCV 采集 VP8 编码"]
        System["System 系统<br/>system/system_info.py<br/>CPU/GPU/电池/网络"]
        Power["PowerPolicy 省电<br/>system/power_policy.py<br/>动态降频策略"]
        Dance["DanceController<br/>extension/dance.py<br/>舞蹈编排"]
        Voice["VoiceBroadcast<br/>extension/voice_broadcast.py<br/>语音喊话"]
        FindDevice["FindDevice<br/>extension/find_device.py<br/>寻找蓝牙设备"]
    end

    subgraph SubServices["Sub Services 子进程服务"]
        Music["MusicPlayer<br/>sub_services/music_player.py<br/>本地音乐播放"]
        DLNA["PyDLNARenderer<br/>sub_services/py_dlna_renderer.py<br/>DLNA 投屏"]
        RemoteCtrl["RemoteControl<br/>sub_services/remote_control.py<br/>遥控子服务"]
        AdvCollector["AdvancedCollector<br/>sub_services/advanced_collector.py<br/>高级数据采集"]
        SoftMgr["SoftwareManager<br/>sub_services/software_manager.py<br/>软件包管理"]
    end

    subgraph Utils["Utils 工具层"]
        Logger["Logger<br/>utils/logger.py<br/>日志管理 (轮转)"]
        TTS["TTSEngine<br/>utils/tts.py<br/>espeak + aplay"]
        QR["QRScanner<br/>utils/qr_scanner.py<br/>二维码扫描"]
    end

    subgraph Hardware["Hardware 硬件抽象层"]
        HW_Motion["MotionBackend<br/>mock / serial / gpio / rosmaster"]
        HW_Camera["CameraBackend<br/>USB cam0 + CSI cam1"]
        HW_Gimbal["GimbalBackend<br/>PWM 舵机控制"]
    end

    Main --> WS
    Main --> RTC
    Main --> MsgHandler
    Main --> MDNS
    Main --> HTTP
    Main --> SvcMgr
    Main --> BindMgr
    Main --> AcctClient
    Main --> PeriDetect

    WS --> MsgHandler
    RTC --> MsgHandler

    MsgHandler --> Motion
    MsgHandler --> Camera
    MsgHandler --> System
    MsgHandler --> Power
    MsgHandler --> Dance
    MsgHandler --> Voice
    MsgHandler --> FindDevice

    SvcMgr --> Music
    SvcMgr --> DLNA
    SvcMgr --> RemoteCtrl
    SvcMgr --> AdvCollector
    SvcMgr --> SoftMgr

    Motion --> HW_Motion
    Camera --> HW_Camera
    Dance --> HW_Gimbal

    MsgHandler -.-> Logger
    Voice -.-> TTS
```

### 模块职责表

| 层 | 模块 | 职责 |
|----|------|------|
| **Core** | WebSocketServer | WebSocket 信令服务器，客户端连接入口，消息收发 :8765 |
| **Core** | WebRTCService | aiortc 视频推流 (VP8) + DataChannel 业务通道 |
| **Core** | MessageHandler | 消息路由中枢，解析客户端指令 → 分发功能模块 |
| **Core** | MDNSService | zeroconf 局域网服务发现 `_wobot._tcp.local.` |
| **Core** | HttpAPIServer | HTTP API :8000 (MJPEG 流、截图、状态查询) |
| **Core** | BindingManager | HMAC-SHA256 绑定认证，PBKDF2 密码验证 |
| **Core** | AccountClient | 对接 wo-bot-account (设备注册 + 心跳 + 绑定证明) |
| **Core** | ServiceManager | 子进程生命周期管理 (启动/停止/重启守护) |
| **Modules** | Motion | 运动控制 (mecanum/differential/ackermann)，多硬件后端 |
| **Modules** | Vision | OpenCV 摄像头采集，双摄像头支持 |
| **Modules** | System | CPU/GPU/内存/电池/网络状态采集 |
| **Modules** | PowerPolicy | 省电策略引擎 (动态降频阈值) |
| **Modules** | Dance/ Voice/ FindDevice | 扩展功能 (舞蹈编排、语音喊话、寻找设备) |
| **SubServices** | Music/ DLNA/ Remote | 独立子进程服务 (音乐、投屏、遥控) |
| **SubServices** | SoftwareManager | 对接 wo-bot-market 软件包管理 |
| **Utils** | Logger/ TTS/ QR | 日志、TTS 语音合成、二维码扫描 |

---

## 三、Web 前端 (wo-bot-web-debug) 架构

```mermaid
graph TB
    subgraph Entry["入口"]
        MainTS["main.ts<br/>createApp → Pinia → Router"]
        AppVue["App.vue<br/>根组件<br/>连接管理 & 视图路由"]
    end

    subgraph Composables["Composables 组合式函数"]
        useWS["useWebSocket<br/>WebSocket 连接管理<br/>认证/分享码/版本校验"]
        useRTC["useWebRTC<br/>WebRTC 媒体传输<br/>视频流 + DataChannel"]
        useDisc["useDiscovery<br/>mDNS + HTTP 设备发现<br/>同网段快速探活"]
        useMock["useMock<br/>Mock 模式<br/>开发/演示"]
        useLog["useDebugLog<br/>调试日志"]
    end

    subgraph Stores["Stores 状态管理 (Pinia)"]
        AppStore["appStore<br/>连接/主题/视图/控制模式<br/>Toast/设置持久化"]
        DevStore["devicesStore<br/>已保存/已发现/当前设备"]
        RobotStore["robotStore<br/>机器人运行时数据<br/>状态/消息/日志/命令"]
    end

    subgraph Views["Views 视图 (14 个)"]
        QuickActions["QuickActions"]
        Status["Status"]
        Remote["Remote 遥控"]
        Logs["Logs"]
        Messages["Messages"]
        Software["Software"]
        Dance["Dance"]
        Map["Map 地图"]
        Gallery["Gallery"]
        Settings["Settings"]
        ProcessMgr["ProcessManager"]
        Music["Music"]
        Config["Config"]
        Bind["Bind 绑定认证"]
    end

    subgraph Components["Components UI 组件"]
        Header["AppHeader<br/>连接状态/控制模式/菜单"]
        Sidebar["AppSidebar<br/>设备列表/新增设备"]
        BottomPanel["BottomPanel<br/>命令日志 + SSH"]
        Footer["AppFooter<br/>版本信息"]
    end

    MainTS --> AppVue
    AppVue --> Header
    AppVue --> Sidebar
    AppVue --> BottomPanel
    AppVue --> Footer

    AppVue --> useWS
    AppVue --> useRTC
    AppVue --> useDisc
    AppVue --> useMock

    useWS --> AppStore
    useRTC --> RobotStore
    useDisc --> DevStore

    AppStore --> Views
    RobotStore --> Views
    DevStore --> Views
```

---

## 四、通信架构与数据流

### 4.1 连接建立流程

```mermaid
sequenceDiagram
    participant Browser as 🌐 浏览器 (Web 控制台)
    participant Robot as 🤖 机器人 (wo-bot-control)
    participant Account as ☁️ wo-bot-account

    Note over Browser, Robot: === 局域网发现 ===
    Browser->>Robot: mDNS 查询 _wobot._tcp.local.
    Robot-->>Browser: 返回 IP + 端口 + 设备信息

    Note over Browser, Robot: === WebSocket 连接 ===
    Browser->>Robot: ws://<ip>:8765 连接
    Robot-->>Browser: connected (robot_id, features, ...)

    alt 已绑定
        Browser->>Robot: auth_request (HMAC 签名)
        Robot-->>Browser: auth_success
    else 未绑定
        Robot-->>Browser: auth_required
        Browser->>Robot: bind_request (密码)
        Robot->>Account: 签发绑定证明
        Robot-->>Browser: bind_success
    end

    Note over Browser, Robot: === WebRTC P2P 建立 ===
    Browser->>Robot: WebSocket: webrtc_offer (SDP)
    Robot-->>Browser: WebSocket: webrtc_answer (SDP)
    Browser->>Robot: ICE Candidate 交换
    Robot-->>Browser: ICE Candidate 交换
    Note over Browser, Robot: P2P 连接建立 ✅
    Browser<-->Robot: DataChannel (业务消息主通道)
    Browser<-->Robot: MediaStream (VP8 视频流)
```

### 4.2 双通道消息策略

```mermaid
graph LR
    subgraph 客户端["客户端"]
        UI["Vue 组件"]
    end

    subgraph 通道["通信通道"]
        direction LR
        WS_Channel["🔵 WebSocket :8765<br/>━━━━━━━━━━━━<br/>始终在线<br/>优先级: 兜底"]
        DC_Channel["🟢 WebRTC DataChannel<br/>━━━━━━━━━━━━<br/>P2P 直连<br/>优先级: 优先"]
        Media_Channel["🔴 WebRTC MediaStream<br/>━━━━━━━━━━━━<br/>VP8 视频流<br/>优先级: 独占"]
    end

    subgraph 机器人["机器人"]
        MsgHandler["MessageHandler"]
        Camera["CameraManager"]
    end

    UI -->|"信令 (offer/answer/ICE)"| WS_Channel
    UI -->|"业务命令 (运动/状态/查询)"| DC_Channel
    UI -.->|"命令降级回退"| WS_Channel
    UI -->|"视频渲染"| Media_Channel

    WS_Channel --> MsgHandler
    DC_Channel --> MsgHandler
    Media_Channel --> Camera
```

### 4.3 命令下发与状态上报

```mermaid
sequenceDiagram
    participant UI as 🖥️ 客户端 UI
    participant DC as DataChannel
    participant WS as WebSocket (兜底)
    participant Handler as MessageHandler
    participant Module as 功能模块 (Motion/Camera/...)

    Note over UI, Module: === 命令下发 (优先 DataChannel) ===
    UI->>DC: { type: "move", direction: "forward", speed: 50 }
    DC->>Handler: 解析消息类型
    Handler->>Module: 调用对应控制器
    Module-->>Handler: 执行结果

    alt DataChannel 可用
        Handler-->>DC: { type: "status", ... }
        DC-->>UI: 状态更新
    else DataChannel 不可用 (降级)
        Handler-->>WS: { type: "status", ... }
        WS-->>UI: 状态更新
    end

    Note over UI, Module: === 主动上报 (状态轮询) ===
    Module->>Handler: 系统状态 (CPU/电池/温度)
    Handler->>DC: { type: "system_info", ... }
    DC->>UI: 仪表盘更新
```

### 4.4 跨网络远程控制 (规划中)

```mermaid
sequenceDiagram
    participant Browser as 🌐 浏览器 (任意网络)
    participant Signal as 📡 wo-bot-signal 信令服务器
    participant TURN as 🔄 coturn TURN/STUN
    participant Robot as 🤖 机器人 (任意网络)

    Note over Browser, Robot: === 信令阶段 ===
    Browser->>Signal: WSS 连接 + 设备查询
    Robot->>Signal: WSS 连接 + 在线注册
    Signal-->>Browser: 设备列表
    Browser->>Signal: 连接请求 → 转发给机器人
    Signal-->>Robot: 连接请求

    Note over Browser, Robot: === WebRTC 协商 (经信令中继) ===
    Browser->>Signal: SDP Offer
    Signal->>Robot: SDP Offer
    Robot->>Signal: SDP Answer
    Signal->>Browser: SDP Answer

    Note over Browser, Robot: === ICE / NAT 穿透 ===
    Browser->>TURN: 获取中继地址
    Robot->>TURN: 获取中继地址

    alt P2P 直连成功
        Browser<-->Robot: WebRTC P2P
    else NAT 严格 (TURN 中继)
        Browser<-->TURN: 视频 + 数据中继
        TURN<-->Robot: 视频 + 数据中继
    end
```

---

## 五、帐号服务 (wo-bot-account) 架构

```mermaid
graph TB
    subgraph 入口["入口"]
        Caddy["🔄 Caddy<br/>反向代理<br/>HTTPS 终止"]
    end

    subgraph 认证["身份认证"]
        Logto["🔐 Logto<br/>OAuth2 / OIDC<br/>用户注册 & 登录"]
    end

    subgraph 业务["业务服务"]
        DeviceAPI["📦 wo-bot-device-api<br/>NestJS + Prisma<br/>设备管理 API :3002"]
    end

    subgraph 管理后台["管理前端"]
        WebUI["🖥️ wo-bot-web<br/>Vue 3<br/>帐号管理界面"]
    end

    subgraph 存储["数据存储"]
        PG[("🗄️ PostgreSQL<br/>用户 & 设备数据")]
        Redis[("⚡ Redis<br/>Session & 缓存")]
    end

    subgraph 编排["容器编排"]
        Docker["🐳 Docker Compose<br/>统一编排"]
    end

    Caddy --> Logto
    Caddy --> DeviceAPI
    Caddy --> WebUI

    DeviceAPI --> Logto
    DeviceAPI --> PG
    DeviceAPI --> Redis

    Docker -.-> Caddy
    Docker -.-> Logto
    Docker -.-> DeviceAPI
    Docker -.-> PG
    Docker -.-> Redis
```

### API 接口

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/devices/register` | POST | 设备注册 (HMAC 签名认证) |
| `/api/devices/heartbeat` | POST | 设备心跳上报 |
| `/api/binding/proof` | POST | 签发绑定证明 (OAuth2 保护) |
| `/api/binding/verify` | POST | 验证绑定关系 |
| `/api/discover` | GET | 查询用户已绑定设备列表 |

---

## 六、部署架构

```mermaid
graph TB
    subgraph 机器人["🤖 机器人 (Jetson Orin NX/AGX)"]
        direction TB
        Systemd["systemd<br/>wobot-control.service<br/>开机自启"]
        Control["wo-bot-control<br/>Python asyncio<br/>主进程"]
        SubProc["子进程群<br/>Music / DLNA / Remote<br/>SoftwareManager"]
        Control --> SubProc
        Systemd --> Control
    end

    subgraph 局域网["🏠 局域网"]
        Browser["浏览器<br/>https://<ip>:9093<br/>或 http://<ip>:5173"]
        MDNS_Discover["mDNS 发现<br/>_wobot._tcp.local."]
    end

    subgraph 云服务器["☁️ 云服务器 (VPS)"]
        direction TB
        DockerHost["Docker Engine"]
        subgraph Containers["容器组"]
            CaddyC["Caddy<br/>反向代理"]
            LogtoC["Logto<br/>认证服务"]
            DeviceAPIC["wo-bot-device-api<br/>NestJS :3002"]
            PGC[("PostgreSQL")]
            RedisC[("Redis")]
        end
        DockerHost --> Containers
    end

    subgraph 市场["🛒 软件市场"]
        MarketSrv["wo-bot-market<br/>Python HTTP :9099<br/>静态文件 + API"]
    end

    Browser <-->|"WebSocket / WebRTC"| Control
    Browser -.->|"mDNS"| MDNS_Discover
    Control -->|"设备注册 & 心跳"| CaddyC
    CaddyC --> DeviceAPIC
    Control -->|"软件查询"| MarketSrv
    Browser -->|"OAuth2 登录"| CaddyC
    CaddyC --> LogtoC
```

---

## 七、技术栈总览

```mermaid
graph LR
    subgraph 机器人端["🤖 机器人端"]
        Python["Python 3.10+"]
        asyncio["asyncio"]
        OpenCV["OpenCV"]
        aiortc["aiortc"]
        websockets["websockets"]
        zeroconf["zeroconf"]
    end

    subgraph Web前端["🌐 Web 前端"]
        Vue["Vue 3"]
        TS["TypeScript 6"]
        Vite["Vite 8"]
        Pinia["Pinia 3"]
        WebRTC_API["WebRTC API"]
        Leaflet["Leaflet"]
    end

    subgraph 移动App["📱 移动 App"]
        Flutter["Flutter 3.44+"]
        Riverpod["Riverpod"]
        GoRouter["GoRouter"]
        flutter_webrtc["flutter_webrtc"]
    end

    subgraph 云端["☁️ 云端"]
        NestJS["NestJS 10"]
        Prisma["Prisma 5"]
        Logto["Logto OIDC"]
        PostgreSQL["PostgreSQL"]
        Redis["Redis"]
    end

    subgraph 部署["🚀 部署"]
        systemd["systemd"]
        Docker["Docker Compose"]
        CaddySrv["Caddy"]
        GHA["GitHub Actions"]
    end
```

---

## 八、未来架构：wo-bot-signal 信令服务器 (Phase 3 详细设计)

> 状态：🔴 规划设计中 | 对应 Phase：R00036 (授权与信令服务器) + R00037 (跨网络远程连接)

### 8.1 设计原则

信令服务器 **不持有用户/设备归属数据**，所有鉴权决策委托给 `wo-bot-account`：
- 信令服务器只负责 **WebSocket 连接管理 + WebRTC 信令转发**
- 用户身份由 wo-bot-account 签发的 JWT 承载，信令服务器验签不验库
- 设备绑定关系由 wo-bot-account 存储，信令服务器每次连接时实时校验
- 信令服务器无数据库，仅内存中维护连接房间表 + Redis 缓存绑定验证结果

### 8.2 信令服务器模块架构

```mermaid
graph TB
    subgraph Entry["入口层"]
        Nginx["🔀 Nginx / Caddy<br/>WSS 终止 + 速率限制<br/>:443"]
    end

    subgraph SignalCore["wo-bot-signal 核心 (Node.js)"]
        WSS["WebSocketHub<br/>ws_hub.ts<br/>━━━━━━━━━━━━━━<br/>连接池管理<br/>心跳检测 (30s)<br/>房间-用户映射"]
        
        AuthMW["AuthMiddleware<br/>auth_middleware.ts<br/>━━━━━━━━━━━━━━<br/>JWT 验签<br/>→ 调用 account 验证<br/>设备归属校验<br/>→ 调用 account /binding/verify"]
        
        SignalRelay["SignalingRelay<br/>signaling_relay.ts<br/>━━━━━━━━━━━━━━<br/>offer/answer/ICE 转发<br/>端到端路由<br/>会话超时 (60s)"]
        
        TurnGen["TurnCredential<br/>turn_credential.ts<br/>━━━━━━━━━━━━━━<br/>coturn HMAC 短期凭证<br/>24h 有效期<br/>按 userId 生成"]
        
        RateLimit["RateLimiter<br/>rate_limiter.ts<br/>━━━━━━━━━━━━━━<br/>单 IP 连接限制<br/>信令消息频率控制<br/>防重放 Nonce 缓存"]
    end

    subgraph External["外部依赖"]
        AccountAPI["☁️ wo-bot-account<br/>━━━━━━━━━━━━━━<br/>/api/auth/verify (JWT 验签)<br/>/api/binding/verify (绑定校验)<br/>/api/devices/status (设备在线状态)"]
        RedisCache[("⚡ Redis<br/>━━━━━━━━━━━━━━<br/>绑定缓存 (5min TTL)<br/>Nonce 防重放 (60s TTL)<br/>连接速率计数")]
        Coturn["🔄 coturn<br/>TURN/STUN :3478<br/>短期凭证验证"]
    end

    Nginx --> WSS
    WSS --> AuthMW
    AuthMW --> AccountAPI
    AuthMW --> RedisCache
    WSS --> SignalRelay
    WSS --> TurnGen
    WSS --> RateLimit
    TurnGen --> Coturn
```

### 8.3 三端鉴权模型

```mermaid
graph TB
    subgraph 信任根["🔑 信任根"]
        LogtoKeys["Logto OAuth2<br/>JWKS 公钥集<br/>(RS256)"]
        RobotSecret["ROBOT_SECRET<br/>预共享密钥<br/>(HMAC-SHA256)"]
    end

    subgraph 客户端鉴权["🖥️ 客户端鉴权"]
        UserLogin["用户 → Logto 登录<br/>→ 获取 Access Token (JWT)"]
        ClientConn["客户端 → Signal WSS<br/>?token=&lt;JWT&gt;"]
        JWT_Verify["Signal 验签：<br/>① 用 Logto JWKS 验证签名<br/>② 检查 exp/iss/aud<br/>③ 提取 userId"]
    end

    subgraph 机器人鉴权["🤖 机器人鉴权"]
        RobotReg["机器人 → wo-bot-account<br/>设备注册 + 心跳 (HMAC)"]
        RobotConn["机器人 → Signal WSS<br/>?role=robot&device_id=X<br/>&timestamp=T&signature=S"]
        HMAC_Verify["Signal 验签：<br/>① 时间戳偏差<60s<br/>② HMAC-SHA256(device_id:timestamp, ROBOT_SECRET)<br/>③ 调 account 验证设备已注册"]
    end

    subgraph 绑定校验["🔗 绑定校验 (Call 时)"]
        CallReq["客户端发起 call → Signal"]
        BindCheck["Signal → account<br/>POST /api/binding/verify<br/>{ userId, deviceId }"]
        CacheHit["命中 Redis → 直接放行"]
        CacheMiss["未命中 → 调 account<br/>结果缓存 5min"]
        Reject["未绑定 → 403<br/>Forbidden"]
    end

    UserLogin --> ClientConn
    ClientConn --> JWT_Verify
    JWT_Verify --> LogtoKeys

    RobotReg --> RobotConn
    RobotConn --> HMAC_Verify
    HMAC_Verify --> RobotSecret

    CallReq --> BindCheck
    BindCheck --> CacheHit
    BindCheck --> CacheMiss
    CacheMiss --> Reject
```

### 8.4 完整远程连接时序

```mermaid
sequenceDiagram
    actor User as 👤 用户 (浏览器/App)
    participant Client as 🖥️ 客户端
    participant Logto as 🔐 Logto (wo-bot-account)
    participant Signal as 📡 wo-bot-signal
    participant Account as 📦 wo-bot-device-api
    participant Robot as 🤖 机器人

    Note over User, Robot: ═══ Phase A: 用户登录 & 鉴权 ═══

    User->>Client: 打开控制台
    Client->>Logto: OAuth2 Authorization Code Flow
    Logto-->>Client: Access Token (JWT, RS256, 1h)
    Logto-->>Client: Refresh Token (7d)

    Client->>Signal: WSS 连接<br/>?token=&lt;JWT&gt;

    Note over Signal: ① JWT 验签<br/>用 Logto JWKS 验证签名<br/>提取 userId + exp
    alt JWT 无效/过期
        Signal-->>Client: 401 Unauthorized
        Client->>Logto: 用 Refresh Token 换新 Access Token
        Client->>Signal: 重试连接
    end

    Signal-->>Client: connected + 设备列表 (已绑定)

    Note over User, Robot: ═══ Phase B: 机器人上线 & 鉴权 ═══

    Robot->>Account: POST /api/devices/heartbeat<br/>(HMAC 签名)
    Account-->>Robot: 在线注册成功

    Robot->>Signal: WSS 连接<br/>?role=robot&device_id=ABC<br/>&timestamp=T&signature=HMAC

    Note over Signal: ② 机器人验签<br/>HMAC-SHA256(device_id:timestamp, ROBOT_SECRET)<br/>时间戳偏差 < 60s

    Signal->>Account: POST /api/devices/verify<br/>{ deviceId: "ABC" }
    Account-->>Signal: { registered: true, ownerId: "U1" }

    Signal-->>Robot: connected (信令通道就绪)
    Note over Signal: 内存注册: rooms["ABC"] = robotWs

    Note over User, Robot: ═══ Phase C: 发起远程连接 ═══

    User->>Client: 点击设备 "客厅小蜗 (ABC)"
    Client->>Signal: { type: "call", deviceId: "ABC" }

    Note over Signal: ③ 绑定校验<br/>Redis 缓存查询 → Miss

    Signal->>Account: POST /api/binding/verify<br/>Authorization: Bearer &lt;JWT&gt;<br/>{ deviceId: "ABC" }

    Note over Account: 查询 userId ↔ deviceId<br/>绑定关系表

    alt 未绑定
        Account-->>Signal: { bound: false }
        Signal-->>Client: { type: "error", code: "NOT_BOUND" }
    else 已绑定
        Account-->>Signal: { bound: true, deviceId: "ABC" }
        Note over Signal: Redis 缓存: bind:U1:ABC = true (5min TTL)

        Signal->>Robot: { type: "call_request", from: "userId" }
        Robot-->>Signal: { type: "call_accept" }

        Note over User, Robot: ═══ Phase D: WebRTC 信令交换 ═══

        Client->>Signal: { type: "offer", target: "ABC", sdp: "..." }
        Signal->>Robot: { type: "offer", from: "userId", sdp: "..." }
        
        Robot->>Signal: { type: "answer", target: "userId", sdp: "..." }
        Signal->>Client: { type: "answer", from: "ABC", sdp: "..." }

        loop ICE Candidate 交换
            Client->>Signal: { type: "ice", target: "ABC", candidate: {...} }
            Signal->>Robot: { type: "ice", from: "userId", candidate: {...} }
            Robot->>Signal: { type: "ice", target: "userId", candidate: {...} }
            Signal->>Client: { type: "ice", from: "ABC", candidate: {...} }
        end

        Note over Client, Robot: ═══ Phase E: P2P 连接建立 ═══

        Client<-->Robot: WebRTC P2P / TURN 中继<br/>DataChannel + 视频流

        Note over Signal: 连接建立后信令服务器<br/>仅维持心跳，不参与数据通道
    end
```

### 8.5 信令服务器内部状态模型

```mermaid
stateDiagram-v2
    [*] --> PendingAuth: WSS 连接到达
    
    PendingAuth --> Authenticated: JWT 验签通过 (客户端)<br/>或 HMAC 验签通过 (机器人)
    PendingAuth --> Rejected: 验签失败 / Token 过期
    Rejected --> [*]: 断开连接
    
    Authenticated --> Registered: 机器人发送 register<br/>(deviceId → ws 映射)
    Authenticated --> Idle: 客户端等待用户操作
    
    Registered --> WaitingOffer: 客户端发起 call
    
    Idle --> Calling: 用户点击设备
    Calling --> WaitingAnswer: offer 已转发给机器人
    Calling --> Timeout: 60s 无响应
    Timeout --> Idle: 返回超时错误
    
    WaitingAnswer --> Connected: answer 已转发给客户端
    WaitingOffer --> Connected: robot 接受呼叫
    
    Connected --> [*]: WebRTC 建立后信令通道降级为心跳
    Connected --> [*]: 任一端断开 WebSocket
```

### 8.6 信令消息协议

#### 客户端 ↔ 信令服务器

| type | 方向 | payload | 说明 |
|------|------|---------|------|
| `call` | C→S | `{ deviceId }` | 发起连接请求 |
| `call_response` | S→C | `{ deviceId, accepted: bool, reason? }` | 机器人应答 |
| `offer` | C↔S | `{ target, sdp }` | SDP Offer (双向) |
| `answer` | C↔S | `{ target, sdp }` | SDP Answer (双向) |
| `ice` | C↔S | `{ target, candidate }` | ICE Candidate (双向) |
| `device_list` | S→C | `[{ deviceId, name, online }]` | 已绑定设备列表 |
| `device_online` | S→C | `{ deviceId }` | 设备上线通知 |
| `device_offline` | S→C | `{ deviceId }` | 设备离线通知 |
| `turn_credentials` | S→C | `{ username, password, urls, ttl }` | TURN 凭证下发 |
| `error` | S→C | `{ code, message }` | 错误响应 |

#### 机器人 ↔ 信令服务器

| type | 方向 | payload | 说明 |
|------|------|---------|------|
| `register` | R→S | `{ deviceId }` | 机器人在线注册 |
| `call_request` | S→R | `{ from: userId }` | 收到连接请求 |
| `call_accept` | R→S | `{}` | 接受连接 |
| `call_reject` | R→S | `{ reason? }` | 拒绝连接 |
| `offer` | R↔S | `{ target: userId, sdp }` | SDP Offer/Answer |
| `ice` | R↔S | `{ target: userId, candidate }` | ICE Candidate |
| `turn_credentials` | S→R | `{ username, password, urls, ttl }` | TURN 凭证下发 |
| `ping` | R↔S | `{}` | 心跳 (30s 间隔) |
| `pong` | R↔S | `{}` | 心跳响应 |

### 8.7 依赖 wo-bot-account 新增 API

信令服务器所需的鉴权 API（需在 wo-bot-device-api 中新增）：

| 端点 | 方法 | 认证 | 说明 |
|------|------|------|------|
| `/api/auth/verify` | POST | 服务间 API Key | 验证 JWT 有效性，返回 `{ userId, exp }` |
| `/api/binding/verify` | POST | Bearer JWT (用户) | 校验 `userId ↔ deviceId` 绑定关系 |
| `/api/devices/verify` | POST | 服务间 API Key | 验证设备是否已注册 `{ deviceId, registered, ownerId }` |
| `/api/devices/list` | GET | Bearer JWT (用户) | 查询用户所有已绑定设备及在线状态 |

### 8.8 Token 层级与流转

```mermaid
graph TB
    subgraph Token类型["🎫 Token 类型"]
        AT["Access Token (JWT, RS256)<br/>━━━━━━━━━━━━━━<br/>签发方: Logto<br/>有效期: 1h<br/>内容: { sub, exp, iss, aud }"]
        RT["Refresh Token (Opaque)<br/>━━━━━━━━━━━━━━<br/>签发方: Logto<br/>有效期: 7d<br/>用途: 换发新 Access Token"]
        SK["Service API Key<br/>━━━━━━━━━━━━━━<br/>签发方: wo-bot-account<br/>有效期: 手动轮换<br/>用途: Signal ↔ Account 服务间调用"]
        RS["ROBOT_SECRET<br/>━━━━━━━━━━━━━━<br/>签发方: 运维手动部署<br/>有效期: 手动轮换<br/>用途: 机器人 HMAC 签名"]
        TC["TURN Credential<br/>━━━━━━━━━━━━━━<br/>签发方: wo-bot-signal<br/>有效期: 24h<br/>用途: coturn 短期认证"]
    end

    subgraph 流转["🔄 Token 流转"]
        direction LR
        LogtoSrv["Logto"] -->|"签发"| AT
        LogtoSrv -->|"签发"| RT
        AccountSrv["wo-bot-account"] -->|"签发"| SK
        Ops["运维"] -->|"部署"| RS
        SignalSrv["wo-bot-signal"] -->|"生成"| TC
    end

    AT -->|"客户端 → Signal<br/>WSS 连接认证"| SignalSrv
    SK -->|"Signal → Account<br/>服务间鉴权"| AccountSrv
    RS -->|"机器人 → Signal<br/>HMAC 签名"| SignalSrv
    TC -->|"客户端/机器人 → coturn<br/>TURN 认证"| CoturnSrv["coturn"]
```

### 8.9 安全纵深防御

```mermaid
graph LR
    subgraph L1["Layer 1: 传输"]
        TLS["TLS 1.3<br/>WSS 加密<br/>Let's Encrypt"]
    end
    
    subgraph L2["Layer 2: 认证"]
        JWT_V["JWT RS256 验签<br/>Logto JWKS"]
        HMAC_V["HMAC-SHA256<br/>ROBOT_SECRET"]
        APIK_V["Service API Key<br/>Header 校验"]
    end

    subgraph L3["Layer 3: 授权"]
        BindCheck_L3["实时绑定校验<br/>userId ↔ deviceId<br/>每次 call 时验证"]
        CacheT["Redis 缓存<br/>5min TTL + 主动失效"]
    end

    subgraph L4["Layer 4: 防护"]
        Rate["速率限制<br/>100 req/min per IP<br/>10 conn/min per IP"]
        Nonce["Nonce 防重放<br/>timestamp ±60s<br/>Redis 去重"]
        Timeout["会话超时<br/>信令协商 60s<br/>心跳超时 90s → 踢出"]
    end

    L1 --> L2 --> L3 --> L4
```

### 8.10 部署拓扑

```mermaid
graph TB
    subgraph VPS["☁️ 云服务器 (VPS)"]
        direction TB
        DockerHost["Docker Engine"]
        
        subgraph SignalStack["wo-bot-signal 栈"]
            SignalNode1["signal-instance-1<br/>Node.js :3001"]
            SignalNode2["signal-instance-2<br/>Node.js :3002"]
        end

        subgraph InfraStack["基础设施栈"]
            NginxC["Nginx<br/>:443 WSS → upstream signal"]
            CoturnC["coturn<br/>:3478 TCP+UDP<br/>:5349 TLS"]
            RedisC[("Redis<br/>:6379<br/>缓存 + 计数")]
        end
    end

    subgraph AccountHost["☁️ wo-bot-account 服务器"]
        AccountAPI_Srv["wo-bot-device-api<br/>:3002<br/>鉴权 API"]
    end

    subgraph Clients["🌐 客户端"]
        Browser["浏览器 WSS"]
        App["App WSS"]
    end

    subgraph Robots["🤖 机器人"]
        Robot1["客厅小蜗 WSS"]
        Robot2["卧室小蜗 WSS"]
    end

    Browser --> NginxC
    App --> NginxC
    Robot1 --> NginxC
    Robot2 --> NginxC

    NginxC --> SignalNode1
    NginxC --> SignalNode2

    SignalNode1 --> RedisC
    SignalNode2 --> RedisC
    SignalNode1 --> AccountAPI_Srv
    SignalNode2 --> AccountAPI_Srv

    SignalNode1 -.->|"生成 TURN 凭证"| CoturnC
    SignalNode2 -.->|"生成 TURN 凭证"| CoturnC

    Browser -.->|"WebRTC 媒体"| CoturnC
    Robot1 -.->|"WebRTC 媒体"| CoturnC
```

### 8.11 信令服务器仓库结构 (规划)

```
wo-bot-signal/
├── src/
│   ├── index.ts                  # 入口：启动 HTTP + WSS 服务
│   ├── config.ts                 # 配置加载 (环境变量)
│   ├── ws_hub.ts                 # WebSocket 连接池 & 房间管理
│   ├── auth_middleware.ts        # 鉴权中间件 (JWT + HMAC)
│   ├── signaling_relay.ts        # WebRTC 信令转发引擎
│   ├── turn_credential.ts        # coturn HMAC 短期凭证生成
│   ├── rate_limiter.ts           # 速率限制 & Nonce 防重放
│   ├── account_client.ts         # wo-bot-account API 客户端
│   └── types.ts                  # 类型定义
├── tests/
│   ├── auth.test.ts
│   ├── signaling.test.ts
│   └── integration.test.ts
├── package.json
├── tsconfig.json
├── Dockerfile
├── docker-compose.yml            # signal + redis + coturn
└── README.md
```

### 8.12 与现有架构的关系

```
                        ┌─────────────────────────────┐
                        │      wo-bot-account          │
                        │  ┌───────┐  ┌─────────────┐ │
                        │  │ Logto │  │ Device API  │ │
                        │  │ OAuth │  │ (绑定存储)   │ │
                        │  └───────┘  └──────┬──────┘ │
                        └────────────────────┼────────┘
                                             │
                    鉴权 API (绑定校验)       │
                                             │
    ┌────────────────────┐                   │
    │   wo-bot-signal    │←──────────────────┘
    │  (信令中继 + 鉴权)  │
    │  Node.js + Redis   │
    └──┬─────────────┬───┘
       │ WSS         │ WSS
       │ (客户端)     │ (机器人)
       ▼             ▼
  ┌─────────┐  ┌──────────────────────┐
  │ 浏览器   │  │  wo-bot-control      │
  │ / App   │  │  + signal_client.py  │ ← 新增模块
  └────┬────┘  └──────────┬───────────┘
       │                  │
       └──── WebRTC ──────┘
       (P2P 或 TURN 中继)

  局域网直连模式 (mDNS + ws://:8765) 保留作为降级方案
```

---

## 九、开发阶段与当前状态

| 阶段 | 内容 | 状态 |
|------|------|:--:|
| **Phase 1** | 核心基础：WebSocket + WebRTC 双通道、视频推流、运动/云台控制、前端 UI | ✅ 完成 |
| **Phase 2** | 交互增强：语音喊话、录音发送、实时通话 | ✅ 完成 |
| **Phase 3** | 跨网络远程控制：本地绑定、信令服务器、跨网络连接 | 🔄 进行中 |
| **Phase 4** | 智能能力：AI 避障、人体跟随、TTS/ASR、灯球音乐同步 | ⏳ 规划 |
| **Phase 5** | 生态拓展：自动充电、红外遥控、多机器人组网、4G/5G、原生 App | ⏳ 规划 |

---

## 十、项目仓库结构一览

```
wo-bot/
├── wo-bot-control/          # 🤖 机器人端控制服务 (Python asyncio)
│   ├── src/
│   │   ├── main.py          #   主入口 WoBotControl
│   │   ├── core/            #   核心服务层 (10 个模块)
│   │   ├── modules/         #   功能模块 (motion/vision/system/extension)
│   │   ├── sub_services/    #   子进程服务 (5 个)
│   │   └── utils/           #   工具层 (logger/tts/qr_scanner)
│   ├── config/config.yaml   #   运行时配置
│   └── scripts/             #   部署/安装脚本
│
├── wo-bot-web-debug/        # 🌐 Web 控制台 (Vue 3 + TypeScript + Vite)
│   └── src/
│       ├── components/      #   UI 组件 (Header/Sidebar/BottomPanel + views/)
│       ├── composables/     #   组合式函数 (useWebSocket/useWebRTC/useDiscovery)
│       ├── stores/          #   Pinia 状态管理 (app/devices/robot)
│       ├── router/          #   Vue Router
│       └── types/           #   TypeScript 类型定义
│
├── wo-bot-account/          # 🔐 帐号与设备管理服务 (NestJS + Logto + Docker)
│   ├── packages/
│   │   ├── wo-bot-device-api/   # NestJS 设备管理 API
│   │   └── wo-bot-web/          # Vue 3 管理前端
│   ├── logto/                   # Logto 认证服务
│   └── docker-compose.yml       # 容器编排
│
├── wo-bot-app/              # 📱 移动端 App (Flutter, 骨架阶段)
├── wo-bot-market/           # 🛒 软件市场 (Python HTTP)
├── wo-bot-signal/           # 📡 信令服务器 (Node.js + Redis, 规划中)
├── wo-bot-wiki/             # 📚 项目方案与文档
├── docs/                    # 📄 公共服务部署指南
├── scripts/                 # 🔧 批量操作脚本
└── secret/                  # 🔑 敏感凭证
```
