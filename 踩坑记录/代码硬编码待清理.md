# 代码硬编码待清理

> 以下代码文件中存在针对特定环境（Antonio 的 Jetson）的硬编码值，需要改为配置化或环境变量。
> 创建时间：2026-07-05

---

## 1. wo-bot-control/config/config.yaml

**文件**：`wo-bot-control/config/config.yaml#27`

```yaml
advertised_ip: "192.168.1.47"  # ← 硬编码，应通过环境变量或启动参数注入
```

**建议**：改为 `advertised_ip: "${JETSON_IP:-127.0.0.1}"` 或从系统命令自动检测。

---

## 2. wo-bot-control/scripts/deploy.sh

**文件**：`wo-bot-control/scripts/deploy.sh#12,#25`

```bash
REMOTE_HOST="192.168.1.47"  # ← 硬编码，应通过命令行参数或配置文件传入
...
REMOTE_HOST="192.168.1.47"  # ← 重复定义
```

**建议**：已有 `--jetson` 参数但只做开关，应扩展为支持 `--host <IP>` 指定目标地址。

---

## 3. wo-bot-web-debug/src/components/views/MusicView.vue

**文件**：`wo-bot-web-debug/src/components/views/MusicView.vue#283`

```vue
<span v-else>地址: rtmp://192.168.1.47/live/wobot</span>
```

**建议**：RTMP 地址应从 WebSocket 连接的 host 动态构建，而非写死。
