# 各子项目代码规范问题修复

如果你发现本周已经有运行记录了，则不需要执行

## 任务内容

wo-bot 各子项目代码规范问题修复：针对配置了 GitHub Actions 的项目进行代码规范修复，先到各仓库对应的GitHub Actions 流水线看看执行结果，如果有问题，先根据各子项目内配置的代码规范配置，在本地识别出问题后对代码规范问题进行修复，然后在本地模拟运行流水线打包，打包无问题后提交并推送代码。然后到GitHub Actions页面查看是否还有遗留问题。继续修复，修复代码质量问题要确保不影响正常功能。

上述任务运行完后填写本文件的运行记录部分，时间倒序

## rules
wo-bot-wiki/AGENT.md

## 运行记录

｜ 日期 ｜ 状态 ｜
｜ --- ｜ --- ｜
｜ 2026-08-23 ｜ 已完成，本周无新增问题，无需修复与推送。各子项目本地完整模拟 CI 全部通过：wo-bot-control（ruff 0.16.0 check/format + mypy 1.15 43 文件 + pytest 19 项通过，已同步远端 4 个提交）；wo-bot-web-debug（ESLint + Prettier + vue-tsc type-check + vite build 通过）；wo-bot-market（ruff check + mypy server.py 通过）；wo-bot-signal（ESLint + Prettier + tsc typecheck/build 通过）；wo-bot-account（device-api ESLint + prisma generate + nest build、web ESLint + vite build 通过）。wo-bot-app 无 Git，不在范围内。 ｜
｜ 2026-08-16 ｜ 已完成，本周无新增问题，无需修复与推送。各子项目本地完整模拟 CI 全部通过：wo-bot-control（ruff 0.16.0 check/format + mypy 1.15 + pytest 19 项通过）；wo-bot-web-debug（ESLint + Prettier + vue-tsc type-check + vite build 通过）；wo-bot-market（ruff 0.16.0 check/format + mypy 通过）；wo-bot-signal（ESLint + Prettier + tsc typecheck/build 通过）；wo-bot-account（device-api ESLint + prisma generate + nest build、web ESLint + vite build 通过）。wo-bot-app 无 Git，不在范围内。 ｜
｜ 2026-08-09 ｜ 已完成并推送，CI 全部通过。wo-bot-control：ruff format 2处（ir_controller.py + collector.py）+ mypy 类型错误 7处（_to_thread redefinition 1处 + _lock None check 2处 + result dict 类型注解 1处 + websocket_server discard arg-type 2处）；wo-bot-web-debug：Prettier 4个文件格式化（IRRemoteView.vue + useIRRemote.ts + useWebSocket.ts + useMock.ts）；wo-bot-market：ruff format 1处（server.py）。wo-bot-signal 无新增问题。wo-bot-account 无新增问题。wo-bot-app 无 Git，不在范围内。 ｜
｜ 2026-08-02 ｜ 已完成并推送，CI 全部通过。wo-bot-control：ruff lint 修复 11 处（I001 import排序 3处 + UP035/Callable 1处 + B007 未使用变量 1处 + SIM108 三元表达式 1处 + SIM117 with合并 1处 + UP037 引号类型注解 1处 + F401 未使用import 2处 + F841 未使用变量 1处）、ruff format 3 文件；wo-bot-web-debug：ESLint vue/attributes-order 1处（SensorChartView.vue ref属性顺序）+ Prettier 4 文件。本地完整模拟 CI（ruff/ESLint + format/Prettier + mypy/vue-tsc + pytest/build）通过后推送。wo-bot-signal CI 上次已通过无新增。wo-bot-market CI 上次已通过无新增。wo-bot-account CI 上次已通过无新增。 ｜
｜ 2026-07-26 ｜ 已完成并推送，CI 全部通过。第三轮（本地完整模拟 CI 后修复并推送）：wo-bot-control mypy 2处（signal_client.py _ws 类型注解 + raw_msg str|bytes 转换）；wo-bot-web-debug ESLint vue/attributes-order 3处（RemoteView.vue title属性顺序）；wo-bot-signal ESLint unused-vars 3处（移除 getOnlineDevices/isDeviceOnline/Role）。wo-bot-market 无新增问题。全部项目本地完整模拟 CI（ruff/ESLint + format/Prettier + mypy/tsc + pytest/build）通过后推送。 ｜
｜ 2026-07-19 ｜ 已完成。wo-bot-control：修复 ruff UP037（3处引号类型注解）+ mypy 类型错误（account_client.py _sign arg-type 1处、union-attr 2处）、ruff format（1文件）；CI Run #34 通过。wo-bot-web-debug：ESLint/Prettier/vue-tsc 本地均通过，无新增问题，无需修复。wo-bot-app 无 GitHub Actions，不在范围内。 ｜
｜ 2026-07-12 ｜ 已完成。wo-bot-control：修复 ruff lint（SIM102/SIM110/SIM103 共9处）、ruff format（7文件）、mypy 类型错误（voice_broadcast Queue 类型、find_device float 注解、websocket_server list 注解、qr_scanner _cv2 类型注解）；CI Run #33 通过。wo-bot-web-debug：修复 ESLint（3 unused imports + 3 vue/no-mutating-props）、Prettier（11文件格式化）、TypeScript 类型错误（4处）、package-lock.json 同步、CI Node 版本升级到 24；CI Run #24 通过。wo-bot-app 无 GitHub Actions，不在范围内。 ｜


